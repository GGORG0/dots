[Service]
ExecStartPost=/bin/sh -c 'systemctl --user set-environment SSH_AUTH_SOCK=%t/ssh-agent.socket'
ExecStartPost=/bin/sh -c 'ssh-add ~/.ssh/yk*[0-9]'
ExecStopPost=systemctl --user unset-environment SSH_AUTH_SOCK

[Install]
WantedBy=default.target
